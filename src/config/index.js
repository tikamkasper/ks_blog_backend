module.exports = {
  PORT,
  DB_NAME,
  MONGODB_URI,
  CORS_ORIGIN,

  REFRESH_TOKEN_SECRET,
  REFRESH_TOKEN_EXPIRY,

  TWILIO_ACCOUNT_SID,
  TWILIO_AUTH_TOKEN,
  TWILIO_PHONE_NUMBER,

  NODE_ENV,

  EMAIL,
  EMAIL_PASSWORD,

  CLOUDINARY_CLOUD_NAME,
  CLOUDINARY_API_KEY,
  CLOUDINARY_API_SECRET,
} = process.env;
