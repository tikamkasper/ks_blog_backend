module.exports = {
  PORT,
  DB_NAME,
  MONGODB_URI,

  REFRESH_TOKEN_SECRET,
  REFRESH_TOKEN_EXPIRY,

  NODE_ENV,

  CLOUDINARY_CLOUD_NAME,
  CLOUDINARY_API_KEY,
  CLOUDINARY_API_SECRET,
} = process.env;
